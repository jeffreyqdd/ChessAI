#include "tools/bitboard.h"
#include <iostream>
using namespace std;


int main() { 
    printf("hello world\n");
    cypher::printBitboard(9187201950435737471);
    //cypher::printBitboard(cypher::maskPawnAttacks(cypher::h4, cypher::WHITE));
    // cypher::set_bit(bitboard, cypher::f2);
    // cypher::printBitboard( bitboard );
    // cypher::pop_bit(bitboard, cypher::f2);
    // cypher::printBitboard( bitboard );
    // cypher::pop_bit(bitboard, cypher::f2);
    // cypher::printBitboard( bitboard );
    // cypher::pop_bit(bitboard, cypher::f2);
    // cypher::printBitboard( bitboard );
    

    return 0;
}